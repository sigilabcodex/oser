# Markdown Example

This Markdown document imports into the OSER document model with *emphasis*, **strong text**, `inline code`, and [a link](https://example.com).

## Lists

- One unordered item
- Another unordered item

1. First ordered item
2. Second ordered item

> A blockquote remains semantic.

```ts
export const answer = 42;
```

---

Final paragraph after a horizontal rule.
