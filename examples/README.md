# Examples

This directory will contain small source documents and rendering fixtures once implementation begins.

For the seed phase, no example renderer output is included. Future examples should stay focused and should demonstrate one concept at a time, such as:

- basic article structure
- figures and captions
- notes and references
- paged preview input
- EPUB-oriented document metadata

Generated outputs should be treated as derived artifacts unless a specific fixture is needed for tests.
