# Project Model

Shared TypeScript contracts for OSER project-level manifests.

This package defines the first operational Phase 1 project manifest shape used by `@oser/project-scanner`. It does not scan files by itself and does not depend on renderers, Studio, or external adapter packages.
