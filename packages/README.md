# Packages

This directory is reserved for future OSER packages.

No packages or runtime dependencies are added in the seed phase.

Possible future package boundaries:

- parser
- document model
- semantic HTML renderer
- editorial CSS presets
- paged preview support
- PDF export adapter
- EPUB export adapter
- CLI

Final package names and boundaries should follow the implementation once the architecture is validated.
